# Normalize text files to LF in the repo; check out native on each platform.
* text=auto eol=lf

# Treat binary assets as binary (no EOL/normalization).
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.tiff binary
*.ico binary
*.icns binary
*.pdf binary
